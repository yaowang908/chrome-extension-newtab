import React from "react";
import ReactDOM from "react-dom";

import logo from '../../../assets/icon.png';

export const Header = () => {

  return (
    <div>
      <img src={logo} alt="new tab" />
    </div>
  )
};

